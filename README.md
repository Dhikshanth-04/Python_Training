Strings in python;
- Strings are sequence of characters enclosed within single or double quotes. 
- There are multi line strings where enclosed within triple quotes too.
str1 = "hello"
str2 = 'helo'
str3 = '''hello
        world'''
- Indexing : Iterating through index position, negative indices are also allowed
print(str1[2:2:1])// [initial index : Last index-1 : increament/decrement]

Lists in python;
- Inbuilt data structure, which is mutable and insertion order is maintained, and its index based structure.
- Can be created using two different methods;

1. fruits = ["apple", "mango", "dates"]
2. gadgets = list("phone", "lap")
fruits.append("banana")
fruits2 = ["orange", "pineapple"]
fruits.extend(fruits2)
print(fruits)

- Single element can be deleted using del in list

Tuples in python;
- They are immutable, ordered and can hold different data types
- Single element can be deleted using del


